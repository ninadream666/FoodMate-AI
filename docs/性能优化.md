 二、优化方案

  1. 网络请求优化

  1.1 添加请求缓存层 (apiClient.js)
  // 使用内存缓存 + AsyncStorage 持久化
  const requestCache = new Map();
  const CACHE_TTL = {
    merchants: 2 * 60 * 1000,     // 商家数据 2 分钟
    recommendations: 5 * 60 * 1000, // 推荐数据 5 分钟
    weather: 10 * 60 * 1000,       // 天气 10 分钟
  };

  1.2 添加防抖机制 (HomeScreen.tsx)
  // 位置变化防抖：500ms 内多次变化只请求一次
  const debouncedLoadData = useMemo(
    () => debounce(loadData, 500),
    []
  );

  1.3 请求合并
  - 合并多个小请求为批量 API
  - 使用 Promise.all 并行请求独立数据

  ---
  2. 图片加载优化

  2.1 使用 react-native-fast-image
  import FastImage from 'react-native-fast-image';

  <FastImage
    source={{
      uri: imageUrl,
      priority: FastImage.priority.normal,
      cache: FastImage.cacheControl.immutable,
    }}
    style={styles.image}
    resizeMode={FastImage.resizeMode.cover}
  />

  2.2 图片预加载
  // 在列表渲染前预加载图片
  FastImage.preload(merchantList.map(m => ({ uri: m.imageUrl    
   })));

  2.3 添加图片压缩参数
  // 对 Google Drive 图片添加尺寸参数
  const getOptimizedUrl = (url, width = 300) => {
    if (url.includes('googleusercontent.com')) {
      return `${url}=w${width}`;
    }
    return url;
  };

  ---
  3. 具体实现计划

| 步骤                                  | 内容                         | 涉及文件
| 1                                    | 安装 react-native-fast-image | package.json   |
| 2                                    | 创建缓存工具类               | 新建cacheUtils.js                        |
| 3                                    | 改造 apiClient.js 添加缓存   | apiClient.js   |
| 4                                    | HomeScreen 添加防抖          | HomeScreen.tsx |
| 5                                    | 替换 Image 为 FastImage        RestaurantCard.tsx, MenuListItem.tsx |
| 6                                    | 优化图片 URL                 |merchantService.js                   |

  ---
  三、预期效果

  | 指标           | 优化前           | 优化后          |
  | -------------- | ---------------- | --------------- |
  | 首页网络请求数 | 每次刷新 3-4 次  | 缓存命中时 0 次 |
  |                |
  | 图片重复加载   | 每次都重新下载   | 缓存后立即显示  |
  |                |
  | 位置变化请求   | 每 100m 触发一次 | 防抖合并，500ms |
  | 内只请求一次   |
  | 流量消耗       | 高               | 预计降低 50%+   |

  ---
  是否需要我立即开始实施这些优化？我可以：

  1. 完整实施 - 实现所有优化项
  2. 分步实施 - 先做最关键的缓存和防抖
  3. 仅网络优化 - 只优化网络请求部分
  4. 仅图片优化 - 只优化图片加载部分



     ☒ 安装 react-native-fast-image 依赖
     ☒ 创建缓存工具类 cacheUtils.js
     ☒ 改造 apiClient.js 添加请求缓存
     ☒ HomeScreen 添加防抖和请求优化
     ☒ 创建 OptimizedImage 组件替换原生 Image
     ☒ 优化 RestaurantCard 图片加载
     ☒ 优化 MenuListItem 图片加载
     ☒ 优化 merchantService 图片 URL
     ☐ 优化 OrderListScreen 减少重复请求




     性能优化完成

  已完成的优化内容

  1. 网络请求优化

  新建文件：src/utils/cacheUtils.js
  - 内存缓存 + AsyncStorage 持久化双层缓存
  - 不同数据类型的 TTL 配置（商家 2 分钟、推荐 5 分钟、天气 10 分钟等）
  - 防抖（debounce）和节流（throttle）函数
  - 图片 URL 优化工具

  修改文件：src/services/apiClient.js
  - 添加请求缓存层（GET 请求自动缓存）
  - 请求去重机制（防止同一请求重复发送）
  - 减少调试日志输出（仅在开发模式输出）
  - 支持强制刷新缓存选项

  修改文件：src/screens/HomeScreen.tsx
  - 使用 useCallback 和 useMemo 优化函数引用
  - 位置变化时使用 500ms 防抖
  - 推荐数据缓存（相同位置不重复请求）
  - 下拉刷新时强制跳过缓存
  - 并行请求天气和推荐数据

  修改文件：src/screens/OrderListScreen.tsx
  - 30 秒内重复进入不再请求
  - 订单数据缓存
  - 取消订单后自动清除缓存

  ---
  2. 图片加载优化

  新建文件：src/components/OptimizedImage.tsx
  - 使用 react-native-fast-image 实现高性能图片加载
  - 自动缓存图片（永久缓存策略）
  - 自动压缩 Google 图片 URL（添加宽度参数）
  - 加载中指示器
  - 加载失败自动使用 fallback 图片
  - 支持图片预加载

  修改文件：src/components/RestaurantCard.tsx
  - 使用 OptimizedImage 替换原生 Image
  - 使用 memo 避免不必要的重渲染
  - 使用 useMemo 缓存随机默认图片
  - 简化默认图片 URL（使用 Unsplash）

  修改文件：src/components/MenuListItem.tsx
  - 使用 OptimizedImage 替换原生 Image
  - 使用 memo 优化渲染
  - 简化默认图片 URL

  修改文件：src/services/merchantService.js
  - 将超长 Google 图片 URL 替换为短 URL
  - 使用 Unsplash 作为备用图片源

  ---
  预期效果

  | 指标         | 优化前            | 优化后             |
  | ------------ | ----------------- | ------------------ |
  | 首页网络请求 | 每次进入都请求    | 5 分钟内缓存命中   |
  | 位置变化请求 | 每次变化都触发    | 500ms 防抖合并     |
  | 订单列表请求 | 每次 focus 都请求 | 30 秒内跳过        |
  | 图片加载     | 无缓存，重复下载  | 永久缓存，立即显示 |
  | 图片大小     | 原始尺寸          | 压缩到指定宽度     |
  | 默认图片 URL | >1000 字符        | <80 字符           |·